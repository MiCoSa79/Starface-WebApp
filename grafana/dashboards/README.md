# Dashboards werden hier abgelegt (JSON-Export aus Grafana).
# Jede Datei unter diesem Ordner wird beim Grafana-Start automatisch geladen.
# Standard-Dashboards: "Telefonie Überblick" (Status + Zeitreihen) — kommt
# mit dem Projekt "Telefonie-Monitoring + Grafana" (Wiki).
